export const respondentsStatusApiEndpoint = (surveyId) => `/api/surveys/${surveyId}/report/status/`;
export const respondentsDataApiEndpoint = (surveyId) => `/api/surveys/${surveyId}/report/status/`;
export const surveyReportApiEndpoint = (surveyId) => `/api/surveys/${surveyId}/report-data/`;
